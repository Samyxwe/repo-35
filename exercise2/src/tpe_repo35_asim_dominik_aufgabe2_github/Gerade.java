package tpe_repo35_asim_dominik_aufgabe2_github;

/**
 * 
 * Gerade wird mit Länge erzeugt.
 * Fläche wird an Figur übergeben.
 * Die Fläche einer Geraden ist immer 0.
 */
class Gerade extends Figur {
	/**
	 * Konstruktor sorgt dafür, dass beim erstellen
	 * einer Figur laenge sofort festgelegt wird
	 * und die flaeche 0 gesetzt wird, da eine gerade keine flaeche hat.
	*/
	Gerade(double laenge){
		/**
		 * Default-Konstruktoraufruf der Figur(double flaeche);
		 */
		super();
	}
}
